
import 'package:flutter/material.dart';

class ItemMenuMostchar extends StatelessWidget {
  const ItemMenuMostchar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(children: [Image.asset(name)],);
  }
}
